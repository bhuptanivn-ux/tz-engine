# BAR Rule — House of Bull / House of Bear Reference

**Purpose:** This is a variant rule set for the TZ engine, built on top of the same OHLC state-machine primitives as the original 37-event rule book (`TZ_ENGINE_RULEBOOK_REFERENCE.md`), but with a different structure hierarchy and an added dual-trend ("house") mechanic. Everything not explicitly overridden below inherits from the original rule book: `THRESH = 0.20` (main qualifying threshold), `ANY = 0.01` (minimum move for HH/LL qualification), and the general RED1/RED2 shared mechanic (§3 of the original).

**Status:** Verified against the user's case-study OHLC datasets (01-01-2021 through 24-04-2021, and 01-01-2022 through 09-03-2022) via `bar_rule_simulator.py`, the reference implementation. **`BAR SL2` has been removed entirely.** REAR is back, in a new role: it is no longer a distinct pre-existing structure alongside TZ GREEN — it is specifically the label for the generation that recovers directly from a *deep* BAR/SAR SL, off that dead generation's own BAR 2/SAR 2 reference (see §8), and the REAR ladder itself now extends to a full REAR RE ENTER rung. **REAR is house-specific: `REAR BUY`/`REAR BUY RE ENTER` under House of Bull, `REAR SELL`/`REAR SELL RE ENTER` under House of Bear** — confirmed by the user (see §14). This revision folds in the two-tier BAR/BAR 2 stop-loss split, ungoverned BAR 2 HH/LL tracking, the outer/inner reference ratchet during shallow-SL recovery (including a fixed gap where escalation to a deep SL was never actually checked while awaiting a shallow recovery), the pullback-persists-past-parent-SL correction, the REAR/fresh-TZ-GREEN permanent dual-track that replaces `BAR SL2`, and — most recently — the correction that **the anchor's (TZ GREEN 2/TZ RED 2's) own SHALLOW SL has no consequence at all** (§1/§2): the anchor's two-tier reference *structure* mirrors BAR/BAR 2, but only its *deep* SL matters, and that deep SL keeps being checked — and keeps fully terminating the lineage, pullback included — every day for as long as the gen hasn't taken over, **regardless of whether a RED1/GREEN1 has already attached** (an earlier version of this fix wrongly also retired the anchor on first pullback attach; corrected against 11-01-2022, where the deep SL had to keep firing mid-pullback) — all confirmed against real dates (see §14), including a real case of both sides of the gen-level dual-track alive at once (§8, §14).

---

## 0. Structure hierarchy (BAR rule, House of Bull)

```
TZ GREEN → TZ GREEN 2 → RED1 → RED2 → [ BAR → BAR 2 ] ↻ (repeating engine)
                                            ↓
                          BAR 2 SL (shallow) → NEW BAR 2 reforms directly (§7)
                          BAR SL (deep)      → permanent dual-track race (§8):
                                                 a) fresh TZ GREEN(N+1) cycle, or
                                                 b) REAR BUY - REAR BUY 2 - RED1 - RED2 - BAR - BAR 2
                                               (whichever reaches its own "2" first is active;
                                                the other goes dormant, not terminated)
```

There is **no branch spawning and no branch-level dormancy at the TZ-GREEN-anchor level** (original §7a/§8 removed entirely) — only one TZ GREEN lineage is ever *searching* at a time. Dormancy *does* exist one level down, between a fresh anchor cycle and a REAR recovery racing off a dead generation (§8) — that is a deliberate, permanent, ongoing dual-track, not a one-time branch.

---

## 1. TZ GREEN(n) — structurally mirrors BAR/BAR 2's two-tier reference split, but the shallow tier has no consequence (§2)

- Formation: Low ≥ PrevLow, High > PrevHigh by ≥ 0.20, Close ≥ PrevHigh.
- **Pre-TZ-GREEN-2**: a single, simple SL — Low breaks ref_low by ≥ 0.20, Close **at or below** it (`<=`, not strict `<`). Ordinary ungoverned HH/LL tracking, both sides, `ANY` = 0.01 threshold.
- **The anchor's reference structure is identical to BAR/BAR 2** (§4–§7): TZ GREEN 2's formation freezes TZ GREEN's own reference Low as the *outer* threshold and starts TZ GREEN 2's own fresh *inner* reference Low, exactly like BAR 2 freezing BAR's own reference. This replaces an earlier version of this spec that gave TZ GREEN 2 only a single relabeled SL with no outer/inner split — confirmed wrong via exact numbers (see §14). **But its two SL tiers do not behave like BAR/BAR 2's — see §2.**
- RED(n) itself does not exist in this chain (see §3) — replaced by RED1/RED2 attaching directly after TZ GREEN 2.

## 2. TZ GREEN 2(n) — same reference structure as BAR 2, but the shallow SL is a non-event

- Formation: current-day High > TZ GREEN's reference High by ≥ 0.20, current-day Low ≥ PrevLow, current-day Close ≥ TZ GREEN's reference High. Freezes TZ GREEN's own reference Low as the outer threshold; TZ GREEN 2 starts its own fresh inner reference Low/High from the formation candle's own Low/High.
- **NONE of TZ GREEN 2/TZ RED 2's own "2"-suffixed HH/LL/SL are ever printed — confirmed by the user, correcting two successive earlier drafts of this rule.** Draft 1 mirrored BAR 2 exactly (ungoverned dual HH/LL — wrong). Draft 2 "governed" it to print only the favorable-inner side (`TZ GREEN 2 HH` bullish / `TZ RED 2 LL` bearish) — also wrong, confirmed against 06-01/14-01/09-03-2022: **"once TZ RED 2 occurs, TZ RED 2 SL/LL/HH does not matter... no need to record TZ RED 2 HH/LL/SL."** The inner reference is still computed internally (frozen at whatever it was set to at "2" formation, never updated again) purely so the deep-SL threshold calculation below has both its inputs — it is simply never surfaced in the report. A repeat `TZ RED 2 SL` is additionally wrong on its own terms (confirmed against 10-01-2022): an SL cannot recur without an intervening re-entry, and the anchor's shallow SL has no re-entry concept at all (unlike the gen's `BAR 2 SL` → `BAR 2` recovery, §7).
- **The OUTER reference is the only thing about the anchor that keeps printing post-"2", and it does so continuously — the opposite of BAR/SAR's frozen outer.** Once TZ GREEN 2/TZ RED 2 forms, the anchor's OUTER (the pre-2 anchor's own reference, frozen at the moment of "2" formation) keeps extending on the ADVERSE side for as long as the anchor is tracked at all: `TZ GREEN LL` (bearish-direction adverse move against a bullish anchor) / `TZ RED HH` (bullish-direction adverse move against a bearish anchor), same `ANY` = 0.01 threshold, no Low/Close requirement — **"TZ RED HH will only matter."** This directly feeds the deep-SL threshold (`min`/`max` of outer and inner) so it always reflects the true historical extreme even though the inner side is frozen (the outer's own independent, continuous ratchet catches up to and then exceeds any stale inner value once price extends far enough) — confirmed against 14-02-2022 (`TZ RED HH` needed so the deep threshold correctly reflects that day's new high) — and it is also the reference the support/resistance highlighting reads (§15): `TZ GREEN LL`/`TZ RED HH` are exactly the "base-level" events that rule already named.
- **Two-tier detection internally, but only the deep tier has ANY consequence — including being printed at all.**
  - `TZ GREEN 2 SL` (shallow): still computed (Low breaks TZ GREEN 2's own inner threshold by ≥ 0.20, Close doesn't reclaim, *and* the deep threshold isn't also breached the same day) but **never printed and never acted on**, per the correction above. RED1/RED2/BAR/BAR 2/REAR BUY/REAR BUY 2/etc. (or their House-of-Bear mirrors) never depend on the anchor's own High/Low/reference for *their own* trigger conditions (§3) — but that is a separate fact from whether the anchor keeps tracking for its own sake, which it does, silently (see the deep tier, next).
  - `TZ GREEN SL` (deep): Low breaks `min(TZ GREEN's frozen outer ref_low, TZ GREEN 2's current inner ref_low)` by ≥ 0.20, Close doesn't reclaim. **This is the only anchor-level SL that makes a difference, and it is checked every single day for as long as the anchor is being tracked at all — including on a day where a RED1/GREEN1 pullback is already attached and independently resolving.** It is a total, unconditional termination — "complete lineage dies," pullback included, even if that pullback's own RED2/GREEN2 condition would otherwise also have confirmed that same day (the pullback simply never gets the chance to print — confirmed by the user against 11-01-2022: `TZ RED SL` fires the same day GREEN2's own condition would have held, and GREEN2 does not print at all). There is no REAR-equivalent recovery for the anchor: a fresh `TZ GREEN(N+1)` can only form from here via the ordinary ground-up formation breakout.
  - **Same-day priority, confirmed explicitly**: if both the shallow and deep thresholds are breached the same day, `TZ GREEN SL` (deep) is what gets recorded — never `TZ GREEN 2 SL` alongside it.
- **Gates RED1 once**: RED1 cannot attach until TZ GREEN 2 has formed at least once. This does **not** mean TZ GREEN 2's own state stops mattering once that gate is passed — its own deep SL still governs the whole lineage's survival (previous bullet). What RED1/RED2/BAR/etc.'s own *trigger conditions* never do is read TZ GREEN 2's High/Low/reference directly — those conditions are self-contained, based on the raw price series (§3–§8).
- **No soft/close-based invalidation**: a Close below TZ GREEN 2's reference during RED1/RED2 formation does not end the cycle. Only the anchor's own deep SL can terminate everything.
- **Retirement — exactly one trigger**: the anchor's own SL/stage2/HH-LL tracking stops being checked or printed **permanently** the moment this lineage's *generation* (BAR, or any rung of the REAR BUY/SELL ladder) reaches its own "2" stage for the first time — at that point BAR 2/REAR 2/etc. has its own separate two-tier reference and deep-SL check, and the anchor's own (now stale) one would be a redundant, unrelated failure mode. **A RED1/GREEN1 pullback attaching against the anchor does NOT retire it** — an earlier draft of this spec added pullback-attach as a second retirement trigger; that was wrong, confirmed by the 11-01-2022 case above, where the anchor's own deep SL had to keep firing even with a pullback already attached and resolving. Retirement takes effect the *same day* the gen's own "2" forms but starting from the *next* day (gen-level processing runs before anchor-level processing each day specifically for this).

## 3. RED1(n) / RED2(n)

- RED1 attach: High ≤ PrevHigh, Low < PrevLow by ≥ 0.20, Close ≤ PrevLow.
- RED1 LL ("deepening"): Low breaks `red1.ref_low` further, without the full RED2 conjunction holding.
- RED1 HH ("weak extension", tracked per the original book's §3 even though it's the opposite side from the pullback's own direction): High extends above `red1.ref_high`.
- RED2: Low ≤ `red1.ref_low` AND High ≤ PrevDayHigh AND (PrevHigh − PrevLow) ≥ 0.20 AND Close ≤ `red1.ref_low + eps`.
- RED1 SL: High clears `red1.ref_high` by ≥ 0.20, Close **at or above** `red1.ref_high` (`>=`) → `red1.active = False`.
- **SL always beats RED1/RED2** — checked first every candle.
- **Fresh attach** requires an ACTIVE front — but "active" means "has not suffered its own *deep* SL," not "is a currently-live object": RED1/GREEN1 can attach once the current front's own "…2" exists (TZ GREEN 2 the anchor's first time; BAR 2 on every subsequent generation), **and** that front hasn't been through a *deep* SL. Once a *deep* BAR SL has fired and no fresh BAR has yet reformed, fresh RED1/RED2 attachment is blocked entirely (this does **not** fall back to using the anchor once at least one BAR has ever existed). A *shallow* SL never revokes fresh-attach eligibility at all — at the gen level (BAR 2's own shallow SL) it opens the lightweight recovery window instead (§7); at the anchor level (TZ GREEN 2/TZ RED 2's own shallow SL) it has no effect whatsoever, isn't even printed (§2), and the anchor simply remains eligible, confirmed against 04-02-2022 and 22-03-2021 (a fresh RED1/GREEN1 attaches the same day the anchor's own shallow-SL condition is internally met).
- **Persistence**: an **already-attached** RED1/GREEN1 pullback is NOT cleared when its parent BAR/SAR subsequently dies via SL. It continues resolving toward RED2/GREEN2 (or its own SL) on its own schedule, completely independent of whether its parent generation is still alive. The "active front required" gating in the previous bullet applies only to a *fresh* attach, never to an already-active pullback. This persistence does **not** extend past the *anchor's own deep SL*, though: that is "complete lineage death" (§1/§2), which ends the whole lineage — pullback included — the moment it fires, whether or not a pullback was mid-resolution (e.g. GREEN1 attached 04-02-2022 does not survive TZ RED's own deep SL the very next day, 05-02-2022; a *new*, unrelated GREEN1 attaching later to a *different* TZ RED 2 is not a continuation of it).
- RED2 firing sets `gen_pending` — the trigger that lets the next BAR generation form (see §4).

## 4. BAR(n) — formation

- Forms via a general breakout consuming `gen_pending` (set by RED2): Low ≥ PrevLow, High > PrevHigh by ≥ 0.20, Close ≥ PrevHigh.
- Once at least one BAR has ever formed and later died via SL, a second independent trigger applies: a **fresh** BAR can also form directly off the same plain-breakout shape without requiring a new RED1/RED2 first (the SL-recovery path — see §7).

## 5. BAR 2(n) — formation, ungoverned dual HH/LL tracking

- Formation: current-day High > BAR's reference High by ≥ 0.20, Low ≥ PrevLow, Close ≥ BAR's reference High.
- On formation, BAR's own reference Low is **frozen** as the *outer* threshold (`bar_ref_low`); BAR 2 then starts its own fresh *inner* reference Low from the formation candle's own Low. (House of Bear mirrors this on the High side: SAR's reference High freezes as `bar_ref_high`; SAR 2 starts its own fresh inner reference High.)
- **HH and LL are BOTH recorded, fully, forever — ungoverned.** This is the one deliberate difference from the anchor's TZ GREEN 2/TZ RED 2 governance rule (§2): BAR 2/SAR 2 never silences either side. Every further High extension prints `BAR 2 HH`; every further Low extension prints `BAR 2 LL` — both keep tracking independently for the life of BAR 2, regardless of which side is "favorable."
- Gates the next RED1 attach, same role TZ GREEN 2 played for the first RED1 (§3).

## 6. BAR SL — two-tier split (post-BAR 2)

Before BAR 2 has formed, BAR has a single, simple SL: Low breaks BAR's own ref_low by ≥ 0.20, Close **at or below** it. This is always a "deep" failure (§8) — there is no "shallow" tier and no REAR option until BAR 2 has formed at least once (REAR needs BAR 2's own reference to reform against; if BAR 2 never existed, that reference never existed either).

**Once BAR 2 has formed**, two independent thresholds are live simultaneously:

- **Outer / deep threshold** = `min(BAR's frozen outer ref_low, BAR 2's current inner ref_low)`. Because BAR 2's inner reference keeps ratcheting downward as `BAR 2 LL` fires, this threshold **can get lower over time** even though BAR's own outer reference itself never moves once frozen.
- **Inner / shallow threshold** = BAR 2's own current ref_low.

Each day, both are checked (deep first):

- **`BAR SL` (deep)**: Low breaks the outer/deep threshold by ≥ 0.20, Close doesn't reclaim it. This is the full-restart failure — recovery is via §8 (the REAR/fresh-TZ-GREEN dual-track race).
- **`BAR 2 SL` (shallow)**: Low breaks BAR 2's own inner threshold by ≥ 0.20, Close doesn't reclaim it, *and* the deep threshold is not also breached the same day. Recovery is the lighter-weight path in §7 — a NEW BAR 2 reforming directly, without needing a fresh BAR first.

(House of Bear: mirror on the High side — `SAR SL` deep, `SAR 2 SL` shallow, outer/deep threshold = `max(SAR's frozen outer ref_high, SAR 2's current inner ref_high)`.)

## 7. Shallow-SL recovery — "NEW BAR 2 reforms directly"

When `BAR 2 SL` (shallow) fires, BAR itself is not restarted — the engine instead awaits a **fresh BAR 2** reforming directly, without an intervening plain BAR. The recovery reuses the *same* label it came from — `BAR 2 SL` recovers as `BAR 2` again (not a differently-named "re-enter" event); this holds at every level (§8): `REAR BUY 2 SL` recovers as `REAR BUY 2`, `REAR BUY RE ENTER 2 SL` recovers as `REAR BUY RE ENTER 2` (House of Bear: `REAR SELL 2 SL`/`REAR SELL RE ENTER 2 SL` likewise).

**Exception — RED2 forecloses this recovery, checked continuously**: this lightweight "NEW BAR 2 reforms directly" path is only available for as long as RED1→RED2 has **not** confirmed for this generation. The moment `gen_pending` is set — whether that happens *before* the shallow SL, the *same day* it fires, or on any *later* day while the recovery is still being awaited — the lighter recovery is abandoned entirely (not just skipped at creation time) and a **full fresh BAR** starts instead, directly off `gen_pending`, the moment the ordinary breakout condition next confirms. Confirmed against 08-01-2022 through 19-02-2022: RED1 attaches to BAR 2 on 08-02, RED2 confirms 10-02, `BAR 2 SL` (shallow) fires 11-02 — 12-02 starts a full new `BAR` cycle. Separately, on the Bear side: GREEN1 attaches 12-02, GREEN2 confirms the same day `SAR 2`'s own shallow SL fires (14-02) — since that SL happened before GREEN2 fired that same day, the recovery is created as usual, but is then abandoned the very next eligible check once `gen_pending` is seen: `SAR` forms 16-02, reaching `SAR 2` on 17-02.

**This foreclosure is a gen-level mechanic only — it does NOT apply to the anchor.** An earlier draft of this spec claimed the anchor's own shallow SL (TZ GREEN 2/TZ RED 2's) had an identical "NEW TZ GREEN 2/TZ RED 2 reforms directly" recovery that RED2 could likewise foreclose. That was wrong: per the user's explicit correction, the anchor's shallow SL has **no recovery of its own to foreclose in the first place** — it is a pure non-event (§2). There is nothing here for RED2 to interact with at the anchor level.

- **Recovery**: a fresh breakout above BAR 2's own last (pre-SL) reference High (Low ≥ PrevLow, High > that reference + 0.20, Close ≥ that reference) forms a brand-new BAR 2 immediately — it inherits the still-live outer/deep reference as its own frozen `bar_ref_low`.
- **Escalation**: if instead price breaks the outer/deep threshold (§6) by ≥ 0.20 with Close confirming — whether on the same day the shallow SL fired or on a later day while still awaiting recovery — that converts straight into the full-restart `BAR SL` (deep) path, and §8's escalation ladder begins.
- **Ongoing ratchet while awaiting recovery** — three references move independently, all using the ordinary `ANY` (0.01) threshold, none requiring Low/Close to also hold:
  - The **favorable/recovery-target** reference (BAR 2's own last reference High) — an attempt at reforming BAR 2 that clears this by a little but doesn't fully qualify (Low/Close not required for this ratchet itself) still pushes the target further out. Printed as **`INVALID BAR HH`** (bullish) / **`INVALID BAR LL`** (bearish). This is the same naming convention used for the deep-SL awaiting window in §8, generalized: `INVALID {X} HH/LL` where `X` is the base name of whatever the recovery would be named (its trailing " 2" stripped) — here, `X` = `BAR` because the recovery is `BAR 2`.
  - The **outer** (adverse-side) reference prints `BAR LL` (bullish) / `BAR HH` (bearish) as it extends.
  - The **inner** (adverse-side) reference prints `BAR 2 LL` (bullish) / `BAR 2 HH` (bearish) as it extends.

  All three ratchets are unconditional — they happen whether or not recovery/escalation actually confirms that day, **including the very same day the shallow SL first fires**.
- **Convergence**: once the outer and inner adverse-side references ratchet to the same value, the shallow/deep distinction collapses — any further close past that shared level triggers `BAR SL` (deep) directly, since there is no longer a separate inner threshold to breach first.

(House of Bear mirror: `SAR HH`/`SAR 2 HH` ratchet on the adverse — upside — side while awaiting recovery; `INVALID SAR LL` ratchets the favorable/recovery-target Low; convergence then routes any further close above the adverse level straight to `SAR SL`.)

## 8. Deep BAR SL aftermath — the REAR BUY/SELL ladder / fresh-TZ-GREEN dual-track race

**`BAR SL2` has been removed. The cycle halts (no further HH/LL/SL tracking) on the dead generation itself the moment a deep SL fires** (whether that SL happened before the "2" stage ever formed, or after). Two paths open up, and — unlike anything earlier in this document — **both stay permanently live at once**, not just until one of them wins:

- **(a) Fresh TZ GREEN(N+1) cycle**: a wholly new anchor search, unrelated to the dead generation's own reference — the ordinary base-case breakout (Low ≥ PrevLow, High > PrevHigh + 0.20, Close ≥ PrevHigh), exactly like day 1. This is the *only* available path if the dead generation was a plain `BAR`/`SAR` (not already a ladder rung) that never reached its own "2" stage — the very first promotion onto the ladder still needs `BAR 2`/`SAR 2`'s own reference to exist (confirmed against 15-02-2021: a plain `BAR` dies pre-"2", no REAR possible, only a fresh `TZ GREEN`). **Older-lineage precedence**: if this fresh anchor's own breakout would fire on the exact same day an EXISTING, older lineage's pending path-(b) recovery also resolves, the fresh anchor does NOT form that day at all — only the older lineage's recovery is recorded, and that specific breakout opportunity is forfeited outright, not rescheduled for a later day (confirmed against 10-03-2022/11-03-2022, see §14 — an intermediate version of this fix incorrectly let it form on the very next non-colliding day instead).
- **(b) A recovery window**: opens whenever the dead generation *had* reached its own "2" stage, **or the dead generation was already a rung of the ladder itself** (`REAR BUY`/`SELL` or a `RE ENTER` rung), regardless of whether that particular rung reached its own "2" — confirmed by the user against 06-03-2022/10-03-2022: `REAR BUY` (formed 02-03-2022, own High 315 on 03-03) died via a further deep SL on 06-03 *before* ever reaching `REAR BUY 2`, and still got a recovery window, reforming on 10-03-2022 directly above that plain (pre-"2") 315 reference (High 318 > 315.20, Low 313.5 ≥ PrevLow, Close 317.5 ≥ 315). So the "needs its own '2'" requirement for a recovery to be *possible at all* applies *only* to the first promotion (plain `BAR`/`SAR` → `REAR BUY`/`SELL`); every later rung-to-rung recovery uses whatever reference the dying rung had tracked, "2"-stage or not. The recovery reforms directly above that reference (same breakout shape: Low ≥ PrevLow, High > the reference + 0.20, Close ≥ that reference).
  - **The recovery's NAME only escalates one rung up the ladder if the dying generation actually reached its own "2" before dying — a rung dying BEFORE its own "2" reforms under the exact SAME label, no escalation.** Confirmed by the user against 10-03-2022: `REAR BUY` (never reached `REAR BUY 2`) dies 06-03-2022 and reforms as plain `REAR BUY` again on 10-03 — **not** `REAR BUY RE ENTER`. `RE ENTER` is reserved specifically for escalating off an actual "2": `REAR BUY 2`'s own deep SL is what escalates to `REAR BUY RE ENTER`. **The escalated name is house-specific — House of Bull uses `REAR BUY`, House of Bear uses `REAR SELL`** (confirmed by the user; earlier drafts of this spec used a single generic `REAR` name for both houses, which was wrong):
  - `BAR 2` deep SL → recovers as **`REAR BUY`** (House of Bear: `SAR 2` deep SL → **`REAR SELL`**). A plain `BAR`/`SAR` deep SL *before* reaching its own "2" has no recovery at all — see (a).
  - `REAR BUY` deep SL *before* reaching its own `REAR BUY 2` → recovers as **plain `REAR BUY` again**, same label, no escalation (House of Bear: `REAR SELL` → `REAR SELL` again).
  - `REAR BUY 2` deep SL (i.e. `REAR BUY` DID reach its own "2") → recovers as **`REAR BUY RE ENTER`** (House of Bear: `REAR SELL RE ENTER`).
  - `REAR BUY RE ENTER` deep SL, whether or not it reached `REAR BUY RE ENTER 2` → recovers as **`REAR BUY RE ENTER`** again either way — the ladder **caps here**; it never escalates further (there is no "RE ENTER RE ENTER"). Same capping, House of Bear: `REAR SELL RE ENTER` recovers as `REAR SELL RE ENTER` again regardless.

  Each rung of the ladder is a **full generation**, structurally identical to BAR/BAR 2: `REAR BUY` gets its own **`REAR BUY 2`** (ungoverned dual HH/LL, two-tier SL, shallow-SL recovery — §5–§7, direction-flipped as needed), and `REAR BUY RE ENTER` likewise gets its own **`REAR BUY RE ENTER 2`** with the exact same mechanics (House of Bear: `REAR SELL 2` / `REAR SELL RE ENTER 2`). RED1/RED2 (or GREEN1/GREEN2 under Bear) attach to whichever "2" is currently live exactly as they would to BAR 2. **Once RED2/GREEN2 fires and a fresh generation forms off it, the label reverts to plain `BAR`/`BAR 2` (or `SAR`/`SAR 2`)** regardless of which rung the lineage was just on — REAR BUY/SELL and their RE ENTER rungs are only the labels for the generation immediately recovering from a deep SL, never for an ordinarily-triggered one (see the hierarchy in §0(b): `REAR BUY - REAR BUY 2 - RED1 - RED2 - BAR - BAR 2`).
  - **Recovery target**: the reference to break above (Bull) / below (Bear) is the dead generation's own last FAVORABLE-side reference — its High for a bullish gen (e.g. `BAR 2`'s own High), its Low for a bearish one (e.g. `SAR 2`'s own Low) — confirmed by the user against 21-02-2022/27-02-2022 (BAR 2 formed 21-02 with High 313.5; the REAR BUY recovery target is 313.5, not BAR 2's Low). An earlier version of this code wrongly sourced this from the dead gen's *adverse*-side reference instead; fixed.
  - **While awaiting a rung's recovery**, that same target reference keeps ratcheting on ordinary price action (`ANY` = 0.01 threshold, no Low/Close requirement) — even though the generation is dead, this reference stays live so the recovery remains reachable against an up-to-date target. The ratchet is printed as **`INVALID {X} HH`** (House of Bear mirror: **`INVALID {X} LL`**), where `X` is the name of the SOURCE reference actually being tracked — **not** the name the recovery will become (confirmed by the user: "record INVALID as INVALID BAR 2 HH not INVALID REAR BUY HH" — an earlier version of this spec/code used the target name here, which was wrong). `X` is the dead generation's own "2" name if it had one (`INVALID BAR 2 HH` while awaiting a `BAR`/`BAR 2` deep-SL recovery), or its plain name if it died pre-its-own-"2" as an already-promoted ladder rung (`INVALID REAR BUY HH`, no "2", while awaiting `REAR BUY RE ENTER` off `REAR BUY`'s own pre-"2" reference — confirmed against 09-03-2022). House of Bear mirrors field-literally, e.g. `INVALID SAR 2 LL`.

**Permanent dual-track, not a one-time decision**: whichever of (a)/(b) reaches its own "2" stage first becomes *active*; the other does **not** terminate — it goes **dormant**, exactly mirroring the House of Bull/Bear split (§11) one level down. A dormant lineage keeps existing and can become active again later if the currently-active one later fails — this is recursive and can repeat indefinitely.

**This permanent dual-track is scoped specifically to path (b), the ladder recovery — it does not apply to a bare pre-"2" deep SL.** When a generation dies before its own "2" stage ever formed (no ladder recovery possible, path (a) is the *only* option), its lineage's own anchor is not killed immediately — it keeps ticking its ordinary HH/LL/SL for as long as it takes the fresh anchor search to succeed — but that lineage is **not** kept alive as a permanent competitor once the new anchor forms: it is retired at that point. Only a lineage that reached the ladder (REAR BUY/SELL or their RE ENTER rungs, path (b)) persists indefinitely alongside a fresh TZ GREEN(N+1)/TZ RED(N+1) cycle.

**`gen_pending` (a fresh RED2/GREEN2 firing) is a signal shared across both lineages of a house**, not scoped to just one: any lineage that is itself alive and past its own "2" stage — active or dormant — may independently consume it to form its own next generation. It persists, unconsumed, across days until at least one eligible lineage consumes it (so a lineage that only becomes eligible later can still benefit from an earlier RED2/GREEN2); if multiple lineages are simultaneously eligible the same day, they consume it together, same day (matching the "recorded at the backend" case where a rung's own RED2 also lets the dormant TZ GREEN(N+1)'s own BAR 2(N+1) form the same day).

**Eligibility to consume `gen_pending` requires the front to have ALREADY been past its own "2" stage as of the previous day — not merely as of a transition happening THIS SAME day.** Confirmed by the user against 06-03-2022: a brand-new `TZ RED` anchor reaching its own `TZ RED 2` for the very first time must not, that same day, immediately consume an unrelated `gen_pending` left dangling since 01-03 (from a completely different, already-dead lineage's `GREEN2`) to spawn a fresh `SAR` out of thin air — "`TZ RED 2` & `SAR` together? Not possible." The fresh `SAR` correctly waits until 08-03, the first day `TZ RED 2` has already existed since a prior day. This restriction applies only to fresh-generation formation off `gen_pending` — it does not affect RED1/GREEN1 attaching the same day the anchor's own "2" forms, which is separately confirmed correct (§3).

**Every rung's own deep SL is fully recursive/self-similar**: if a rung's generation later deep-SLs, the exact same race reopens *on that same lineage* — a further TZ GREEN(N+2) vs. the next rung up the ladder (capped at `REAR BUY/SELL RE ENTER`, per above) — **and this happens whether or not that rung reached its own "2"** (see (b) above; only the *first* promotion off a plain `BAR`/`SAR` requires the "2" stage). Separately, if that lineage's own anchor (from when it started life as a fresh TZ GREEN/TZ RED cycle) is still alive, the anchor itself is unaffected and keeps ticking its ordinary HH/LL/SL (§1/§2 tracking was never gated on generation state).

**The anchor's own SL remains an unconditional, total termination of its entire lineage** (§1) — including any generation-level state riding on it, such as a still-pending ladder recovery. This can extinguish a recovery opportunity before it ever gets a chance to fire, even though the recovery's own reference is otherwise unrelated to whatever broke the anchor's own Low. Confirmed case: a BAR SL on 22-03-2021 opened a REAR BUY window (target 617.85); the underlying TZ GREEN anchor (born 17-03) then hit its own SL on 25-03-2021 before REAR BUY ever recovered, killing the pending recovery opportunity along with the rest of that lineage.

**Output disambiguation when both tracks are alive at once**: on a day where 2+ *distinct* lineages of the same house each independently fire their own RED1/RED2 (or GREEN1/GREEN2) pullback event, the plain event name alone would be a visually identical duplicate even though the two firings belong to different structures. The report tags each such event with `(<lineage's current label>)` — the lineage's own gen label if a gen has started (e.g. `REAR BUY`), else its anchor name (e.g. `TZ GREEN`) — so `RED1 + RED1` prints instead as `RED1 (REAR BUY) + RED1 (TZ GREEN)`. This tag is added **only** on days where the ambiguity actually occurs; a single active lineage's pullback events print exactly as before, untagged.

## 9. Summary — the repeating generational engine

There is really **one generational engine**: `BAR → BAR 2 → RED1 → RED2 → next BAR`, repeating for as long as recovery succeeds. TZ GREEN/TZ GREEN 2 is the very start of a cycle. Post-BAR 2, SL is two-tiered (§6/§7): a shallow breach recovers lightly (a NEW BAR 2 reforms directly); a deep breach — whose threshold can itself get deeper over time as BAR 2's own inner reference ratchets past BAR's frozen outer one — opens the permanent dual-track race described in §8 (a fresh TZ GREEN cycle vs. REAR BUY reforming off the dead generation's own reference), which is itself fully recursive on the REAR BUY side. There is no terminal "SL2" state anymore — the engine simply keeps racing between fresh-anchor and REAR-BUY-recovery attempts, forever, one level below the House of Bull/Bear split. (§1–§9 describe this engine as it runs under House of Bull, where the generational label is `BAR`/`BAR 2`/`REAR BUY`/`REAR BUY 2`; §10 defines House of Bear's mirror, where the same engine's label is `SAR`/`SAR 2`/`REAR SELL`/`REAR SELL 2` instead — REAR's own name flips between houses (`BUY` vs `SELL`) exactly like the ordinary generational label does.)

---

## 10. House of Bull vs. House of Bear

Two full mirror-image structures, mostly sharing the same event vocabulary (the one deliberate exception: House of Bull's generational label is `BAR`/`BAR 2`, House of Bear's is `SAR`/`SAR 2`). **Both houses' engines always run, fully independently off the same raw price series, and always log — "active" means whichever house's own chain currently reaches furthest, not that the other stops being tracked or computed.** There is no cross-coupling in the implementation; the "shadow rebirth"/house-switch behavior described in §11 is an emergent consequence of both engines reading the same OHLC series with mirrored formulas, not a separate mechanism layered on top.

**House of Bull** (as built in §1–§9):
```
TZ GREEN → TZ GREEN 2 → RED1 → RED2 → BAR → BAR 2 → RED1 → RED2 → ...
```

**House of Bear** (exact directional mirror — up ↔ down):
```
TZ RED → TZ RED 2 → GREEN1 → GREEN2 → SAR → SAR 2 → GREEN1 → GREEN2 → ...
```

**Naming/condition mapping (House of Bear):**
- `TZ RED` = same condition shape as `RED1` (downside pullback-attach), now serving as the house's own anchor.
- `TZ RED 2` = same condition shape as `RED2`. Governs (silences) the **Low** side of TZ RED's own ongoing tracking; `TZ RED LL` (i.e. the High-side mirror label) keeps tracking.
- `GREEN1` = same condition shape as `TZ GREEN` (upside breakout), now serving as the pullback-within-a-downtrend structure.
- `GREEN2` = same condition shape as `TZ GREEN 2`.
- **`SAR` / `SAR 2`** = House of Bear's own name for the generational-engine label that House of Bull calls `BAR` / `BAR 2` — same condition shape as `TZ RED` / `TZ RED 2` (downside breakdown continuation). **`BAR`/`BAR 2` is Bull-only naming; `SAR`/`SAR 2` is the Bear equivalent.**
- Every downstream mechanic built for Bull in §4–§8 under the name `BAR`/`BAR 2` (the two-tier SL split, the outer/inner ratchet during shallow-SL recovery, the REAR/fresh-anchor dual-track race after a deep SL) applies **exactly, direction-flipped, to `SAR`/`SAR 2`** in House of Bear — read every `BAR`/`BAR 2`/`BAR SL`/`BAR 2 SL` in §4–§8 as `SAR`/`SAR 2`/`SAR SL`/`SAR 2 SL` when applying those rules under House of Bear. **`REAR`'s own name IS house-specific**: House of Bull's ladder is `REAR BUY`/`REAR BUY 2`/`REAR BUY RE ENTER`; House of Bear's is `REAR SELL`/`REAR SELL 2`/`REAR SELL RE ENTER` — same mechanism, side-specific name (confirmed by the user; an earlier draft of this spec had REAR's name NOT flipping between houses, which was wrong). Its ratchet-event naming also flips field-literally (`INVALID REAR SELL LL` instead of `INVALID REAR BUY HH`, tracking SAR 2's own reference Low).

## 11. House-switch mechanic

**Both houses' full engines run continuously and independently off raw price, from day 1, forever — neither is ever dormant, and both are logged every time either fires an event.** "Active" simply means whichever house's own chain reaches further/triggers first; that does not stop the other house's engine from computing and logging.

**Which house is active initially:** determined purely by whichever chain (TZ GREEN's or TZ RED's own independent anchor search) completes its own sequence through to a `BAR`/`SAR` first. Both can — and typically do — produce events on the very same early dates before this is settled (see the case-study table in §13).

**The repeating rebirth/termination cycle** (this is the actual mechanism behind the switch, not a one-shot check):
- Every time the *active* house produces a fresh pullback (RED1→RED2 for an active House of Bull; GREEN1→GREEN2 for an active House of Bear), that identical price action *simultaneously* forms a fresh anchor pair for the *non-active* house (`TZ RED`/`TZ RED 2`, or `TZ GREEN`/`TZ GREEN 2`, respectively) — logged under both houses the same day.
- If the active house's *next* continuation (`BAR`/`BAR 2`) then closes decisively past that fresh non-active anchor's reference (above it for a TZ RED anchor being cleared by an upside BAR; below it for a TZ GREEN anchor being cleared by a downside SAR), the non-active anchor is **terminated** right there.
- The very next time the active house produces another fresh pullback, the non-active anchor is **reborn** from scratch, and the cycle repeats.
- The **switch fires** the first time this cycle breaks the other way: the active house's continuation (`BAR`/`BAR 2` for Bull, `SAR`/`SAR 2` for Bear) **fails** to clear the live non-active anchor's reference, **and** the active house's *next* pullback (RED1→RED2 / GREEN1→GREEN2) then confirms. At that exact point the non-active house takes over as active — and that confirming pullback is relabeled under the *new* active house's own naming.

**Worked trace (confirmed), Bull active initially, two rebirth cycles before the switch:**
```
TZ GREEN - TZ GREEN 2 - RED1 - RED2 - BAR                [Bull's own chain reaches BAR first -> Bull active]
              (RED1/RED2 simultaneously = TZ RED/TZ RED2 for Bear)
           BAR closes above TZ RED's reference high      -> Bear's TZ RED terminated (cycle 1 ends)

... BAR 2 ...
new RED1 - RED2                                          (simultaneously = fresh TZ RED/TZ RED2 for Bear, reborn)
BAR - BAR 2                                               (simultaneously = GREEN1/GREEN2 for Bear)
           BAR/BAR 2 does NOT close above TZ RED's ref high -> Bear's TZ RED SURVIVES (cycle 2 does not terminate)
new RED1 - RED2                                          -> this RED1/RED2 IS Bear's own SAR/SAR 2
                                                              => SWITCH: House of Bear becomes active here
```

**Bear → Bull switch is the exact symmetric mirror**, using GREEN1/GREEN2 pullbacks, downside BAR/BAR 2 continuations, and TZ GREEN/TZ GREEN 2 as the reborn/terminated non-active anchor.

**After a switch — reclaiming active status is different from mere tracking.** The house that just lost active status does **not** stop being tracked or logged. But to become the *active* house again, that house must complete a **brand-new anchor from scratch** (a fresh `TZ GREEN` for Bull, a fresh `TZ RED` for Bear), not a continuation of its old pre-switch lineage — unless it instead wins the §8 dual-track race via REAR, which is the other route back to being active without a brand-new anchor.

## 12. Output format — House column

A **House** column is added alongside the Event column in the report output.

- Every date lists **every** house whose engine produced an event that day — `BULL`, `BEAR`, or `BULL + BEAR` — with the corresponding event(s) listed side by side (e.g., `RED1 + TZ RED`, `BAR 2 SL + TZ RED`, `BAR + GREEN1`).
- On the exact date a house-switch confirms, the event printed for that date uses the **new** house's naming for that step, not the old house's.
- This is a continuous, permanent feature of the output — both houses' events keep appearing side-by-side for the life of the dataset, per §11's repeating rebirth/termination cycle, not only before the first switch ever happens.

## 13. Worked case-study excerpts (from the user, confirmed correct)

**Early sequencing (01/01–15/01):**
```
DATE      HOUSE OF     EVENT
01/01     BULL         TZ GREEN
02/01     BEAR         TZ RED
03/01     BULL + BEAR  TZ GREEN 2 + TZ RED SL
05/01     BULL + BEAR  RED 1 + TZ RED
07/01     BULL + BEAR  RED 2 + TZ RED 2
09/01     BULL + BEAR  BAR + TZ RED HH + GREEN 1
11/01     BULL + BEAR  BAR SL + GREEN 1 SL
12/01     BULL + BEAR  BAR + GREEN 1
15/01     BULL + BEAR  BAR 2 + GREEN 2
```
Row 03/01 lists `BULL + BEAR` — TZ RED SL is still a House-of-Bear event and must be tagged as such even though Bear has no further live structure that day; a house tag is never dropped just because that house's structure terminated on the same date.

**Deep SL aftermath — pre-BAR2 (no REAR possible, fresh TZ GREEN is the only path), and the outer/inner ratchet:**
```
DATE      HOUSE OF     EVENT
15/02     BULL         BAR SL                 [pre-BAR2 SL: deep by default, no BAR2 reference exists]
17/02     BULL         TZ GREEN               [only path available: fresh anchor from scratch]
03/03     BEAR         SAR 2 SL + SAR HH      [shallow SL; outer ref ALSO ratchets same day]
22/03     BEAR         SAR HH: 616            [outer/deep ref ratchets during awaiting-recovery]
24/03     BEAR         SAR 2 HH: 614          [inner ref ratchets independently, still below outer]
01/04     BEAR         SAR AND SAR 2 HH       [High 616.05 clears BOTH — outer and inner converge;
                                                from here, any close past this level = plain SAR SL]
31/03     BULL + BEAR  GREEN2 fires same day SAR2 SL fires — the already-attached pullback
                        (GREEN1→GREEN2) is NOT cleared by its parent SAR's same-day SL.
```

**TZ RED 2's own "2"-suffixed events never print, but its DEEP SL keeps mattering even mid-pullback (01/2022 case study, confirmed exact numbers, corrected per the user's explicit rule statements):**
```
DATE      HOUSE OF     EVENT
06/01     BULL + BEAR  TZ GREEN + GREEN1        [TZ RED 2's shallow-SL condition is internally met but
                                                 never printed -- GREEN1 attaches against the SAME,
                                                 untouched TZ RED 2 the very same day]
08/01     BULL + BEAR  TZ GREEN SL + GREEN1 SL  [GREEN1 (attached 06/01) hits its OWN SL on its own
                                                 schedule -- nothing to do with TZ RED 2's state, which
                                                 keeps computing internally but prints nothing]
09/01     --           (nothing for Bear -- GREEN1 is dead, no fresh attach qualifies this day)
10/01     BULL + BEAR  TZ GREEN + GREEN1        [a FRESH GREEN1 attach -- not GREEN2 continuing off the
                                                 dead 06/01 GREEN1; a dead pullback cannot resolve into
                                                 GREEN2, only a live one can]
11/01     BULL + BEAR  TZ GREEN 2 + TZ RED SL   [Bull's TZ GREEN 2 forms; SEPARATELY, Bear's anchor
                                                 hits its own DEEP SL (outer/inner converge at 299.85)
                                                 -- this is "complete lineage dies," and it fires even
                                                 though the fresh GREEN1 (attached 10/01) was mid-
                                                 resolution: GREEN2's own condition would also have
                                                 held this same day, but it never gets printed --
                                                 confirmed by the user: "GREEN 2 will have no impact,
                                                 since the cycle will be terminated completely"]
15/01     BULL + BEAR  RED1 + TZ RED            [Bull's RED1 attaches against TZ GREEN 2 (unrelated to
                                                 Bear); Bear starts a wholly FRESH TZ RED anchor search
                                                 from scratch -- NOT "SAR", since the lineage that died
                                                 11/01 never had a generation (SAR) form at all before
                                                 its anchor's own deep SL ended it]
16/01     BULL + BEAR  RED1 HH + TZ RED HH      [Bull's RED1 continues; Bear's brand-new TZ RED (pre-"2")
                                                 ticks its own ordinary HH -- note this is the PRE-"2"
                                                 anchor's own tracking, unaffected by any of the above,
                                                 since this lineage hasn't reached TZ RED 2 yet]
```
(House of Bear mirror: read `TZ RED`/`TZ RED 2` for `TZ GREEN`/`TZ GREEN 2` and `SAR`/`SAR 2` for `BAR`/`BAR 2` throughout — the same dataset produces the Bull-side mirror at the corresponding dates.)

**Two-tier SL and shallow recovery (11/04–14/04):**
```
DATE      HOUSE OF     EVENT
11/04     BULL         BAR 2 HH
12/04     BULL + BEAR  BAR 2 SL + TZ RED       [shallow SL on Bull; independent fresh TZ RED on Bear]
13/04     BEAR         TZ RED HH
14/04     BULL + BEAR  BAR 2 + TZ RED SL       [NEW BAR 2 reforms directly, skipping a plain BAR]
```

## 14. Open items — pending case-study verification

- **The anchor's INNER post-"2" tracking never prints at all (superseding an intermediate "governed, favorable-side-only" fix that was itself still wrong), and its OUTER reference ratchets continuously — both confirmed by the user.** The inner reference is computed only to feed the deep-SL threshold internally (frozen at "2"-formation value, never updated or printed again — not even the favorable side); confirmed against 06-01/14-01/09-03-2022 ("no need to record TZ RED 2 HH/LL/SL") and against 10-01-2022 (a repeated `TZ RED 2 SL` is additionally wrong since an SL cannot recur without a re-entry, and the anchor has none). Separately, the OUTER reference — frozen forever for the GEN once set (§6, reconfirmed against 25-01 through 30-01-2022) — instead keeps ratcheting continuously on the adverse side for the ANCHOR (`TZ GREEN LL` / `TZ RED HH`, "TZ RED HH will only matter"), confirmed against 14-02-2022 (`TZ RED HH` needed so the deep-SL threshold reflects that day's true extreme). This anchor outer ratchet is also the reference the support/resistance highlighting reads (§15).
- **A gen's own deep SL that fires the same day its pullback would independently resolve to RED2/GREEN2 does not stop that generation's dual-track (§8) from opening — the pullback's RED2/GREEN2 has "no impact" on which recovery path becomes available.** Confirmed against 01-03-2022: `SAR SL` (deep, `SAR 2` already existed) fires the same day `GREEN2` also confirms (the already-attached pullback persists past its parent's SL, §3, unaffected); the dual-track (a fresh `TZ RED` cycle vs. `REAR SELL` escalation, §8) opens exactly as it would without the coincidental `GREEN2` — `GREEN2`'s own `gen_pending` signal does not let this lineage's dead `SAR` immediately reform as a plain `SAR` again, bypassing the dual-track. This was already the code's behavior; the user's note confirms it rather than correcting a bug.
- **A rung of the REAR ladder that dies before reaching its own "2" has no further recovery, full stop — not even a wholly fresh instance of that same rung.** Confirmed against 10-03-2022: `REAR BUY` (formed 02-03-2022) dies via a further deep `REAR BUY SL` on 06-03 before ever reaching `REAR BUY 2` — since that specific rung's own "2" reference never existed, no `REAR BUY` (or anything else on the ladder) can reform for that lineage; only a wholly fresh `TZ GREEN(N+1)` cycle is possible from here (§8's "if a rung's generation SLs before its own '2' stage ever formed, that lineage's generation-path dies permanently").
- **The REAR ladder's own recovery reference was sourced from the wrong side of the dead "X 2", and its awaiting-ratchet was named after the wrong label — both confirmed by the user and fixed.** The recovery target must be the dead generation's own FAVORABLE-side reference (BAR 2's High, SAR 2's Low) — one of the two code paths that create a `rear_recovery` was instead using the dead gen's adverse-side reference, confirmed wrong against 21-02-2022/27-02-2022 (target should be 313.5, BAR 2's own High from 21-02, not its Low). Separately, the "while awaiting" ratchet is now named after the SOURCE being tracked (`INVALID BAR 2 HH`) rather than the escalation's target label (`INVALID REAR BUY HH`), per the user's explicit correction.
- **Fresh-generation formation off a shared, carried-over `gen_pending` now requires the front to have already been past its own "2" stage as of the PREVIOUS day, not merely as of a transition happening that same day** — confirmed by the user against 06-03-2022 ("`TZ RED 2` & `SAR` together? Not possible"): a brand-new anchor reaching its own "2" for the first time must not, the same day, immediately consume an unrelated `gen_pending` signal left over from an already-dead, unrelated lineage's earlier RED2/GREEN2. See §8.
- **The anchor's (TZ GREEN 2/TZ RED 2's) own shallow SL has no consequence, but its DEEP SL keeps mattering unconditionally — confirmed by the user across two rounds of correction.** Round 1: the anchor previously mirrored BAR/BAR 2's two-tier SL *exactly*, including a "NEW TZ GREEN 2/TZ RED 2 reforms directly" recovery window on a shallow SL — wrong; the shallow SL is now a pure log entry with zero effect (§2), confirmed against 06-01-2022 (`TZ RED 2 SL` does not stop GREEN1, attached the same day, from later dying on its own schedule via `GREEN1 SL` on 08-01) and against 22-03-2021 (`TZ GREEN 2 SL` fires and a fresh RED1 attaches against the same, unkilled anchor the same day). Round 2: an intermediate fix over-corrected by ALSO retiring the anchor's tracking the moment a pullback first attached against it — also wrong, confirmed against 11-01-2022: the anchor's own deep SL (`TZ RED SL`) must keep being checked every day regardless, and still fully terminates the lineage — pullback included — even mid-resolution (GREEN1, attached 10-01, would have resolved to GREEN2 this same day, but the anchor's deep SL fires first and the lineage dies before GREEN2 ever prints; this also means no `SAR` forms on 15-01, since the dead lineage never had a generation — a wholly fresh `TZ RED` anchor search starts instead). **This is a substantial correction that changes the computed trajectory for both case-study datasets from any date where an anchor-level SL (shallow or deep) previously behaved differently** — the 2021 dataset in particular has not yet been walked back through with the user date-by-date under the fully-corrected mechanic (only spot-checked here); a full re-verification pass is recommended before treating the 2021 trajectory as re-confirmed.
- **REAR renamed to REAR BUY / REAR SELL, confirmed by the user.** The ladder mechanism itself (§8) is unchanged; only the label is now house-specific instead of a single generic `REAR` shared by both houses.
- **Only the FIRST promotion onto the REAR ladder needs the dying gen's own "2" for a recovery to be possible at all — every later rung-to-rung recovery does not, confirmed by the user against 06-03-2022/10-03-2022.** `REAR BUY` (formed 02-03-2022) died via a further deep SL on 06-03 before ever reaching `REAR BUY 2`; it still got a recovery window, reforming on 10-03-2022 directly above `REAR BUY`'s own plain (pre-"2") High of 315 (from 03-03). This does not reopen the already-confirmed 15-02-2021 case (a plain `BAR` — not yet on the ladder — dying pre-"2" still has no REAR path, only a fresh `TZ GREEN`): the distinction is the FIRST promotion (plain `BAR`/`SAR` → `REAR BUY`/`SELL`) vs. every later rung-to-rung recovery.
- **However, that 10-03-2022 recovery's NAME does not escalate — since `REAR BUY 2` never formed, it reforms as plain `REAR BUY` again, not `REAR BUY RE ENTER`** — confirmed by the user, correcting an intermediate version of this fix that had escalated the name regardless of whether the "2" was reached. `RE ENTER` is reserved for escalating off an actual "2" (§8). This `REAR BUY` (the 10-03-2022 instance) then reached its own `REAR BUY 2` the very next day, 11-03-2022.
- **Older-lineage precedence, generalized across two rounds of correction: a fresh TZ GREEN(N+1)/TZ RED(N+1) breakout that coincides with an EXISTING, older lineage's own recovery resolving the same day is FORFEITED OUTRIGHT, not merely deferred.** Round 1: "older wins" as a pullback-attach-only tiebreak (letting both fronts form, deferring only a later shared RED1/GREEN1) — rejected by the user against 10-03-2022 ("why to consider TZ GREEN? No need"): the fresh anchor must not form at all that day. Round 2: an intermediate fix instead left `awaiting_fresh_anchor` set (`True`), so the fresh anchor simply formed on the very next non-colliding day, 11-03-2022 — **also rejected**: "You cannot adjust to further dates just because it was not shown during earlier dates... does not mean it can be carried forward to next dates just like that." The final, confirmed rule: on the exact day a fresh-anchor breakout coincides with an older lineage's `rear_recovery` resolving, `awaiting_fresh_anchor` is cleared (consumed) WITHOUT ever forming an anchor — the opportunity is gone, not rescheduled. Only a later, genuinely NEW deep SL (on whichever lineage is then active) reopens the fresh-anchor search through the ordinary mechanism. Confirmed against 10-03-2022 through 11-03-2022: `TZ GREEN` never forms at all in this stretch; only `REAR BUY` → `REAR BUY 2` (11-03-2022) print for House of Bull. Implemented via `rear_recovery_would_resolve` gating the top-level fresh-anchor-search block; same-day collisions between two ALREADY-EXISTING lineages (not a fresh-anchor attempt) are unaffected — that's the ordinary permanent dual-track (§8), not this precedence rule.
- **Two simultaneously-alive lineages (the dormancy race itself) — previously observed against 2022 dates with the RED1-tagging convention exercised; those exact dates shifted after the REAR-recovery-reference fix below and need re-confirming.** House of Bull's `BAR SL` (deep) on 22-02-2022 opens a `REAR BUY` window; with the corrected recovery reference (313.5, BAR 2's own High from 21-02 — see the REAR-recovery-reference item above) `REAR BUY` now forms 02-03-2022, but its own generation dies via a further deep `REAR BUY SL` on 06-03 before ever reaching `REAR BUY 2` (pre-"2" SL, no further ladder recovery for that lineage). The dual-track RED1-tagging convention (`RED1 (REAR BUY)` / `RED1 (TZ GREEN)`, §8) is implemented and was previously exercised at now-stale dates; needs re-confirming against the corrected trajectory.
- **`REAR BUY RE ENTER` (the second rung of the ladder) has not yet been observed firing** — no date in either case-study dataset has produced a deep SL on a `REAR BUY`/`REAR BUY 2` (or `REAR SELL`/`REAR SELL 2`) structure itself that had already reached its own "2" (only on plain `BAR`/`SAR`, the anchor, or a REAR BUY/SELL rung that died pre-"2", per the item above). Needs a case study where a REAR BUY/SELL generation's own deep SL fires *after* reaching its own "2" to fully validate that escalation step.
- **Four related bugs found and fixed together, all surfaced by the extended 2022 dataset (through 23-05-2022) and confirmed against exact numbers:**
  1. **A redundant, duplicate fresh-anchor lineage could spawn while an earlier round's fresh-anchor competitor was still alive.** `awaiting_fresh_anchor` was reopened unconditionally by EVERY gen-deep-SL/bar2_recovery-escalation, even when this lineage's own dual-track competitor (spawned by an EARLIER failure of the SAME lineage's ladder) already existed and was alive — producing a spurious THIRD, redundant lineage. Confirmed against 30-04-2022→03-05-2022: a second `TZ RED` should not have formed since the paired `TZ RED` from 27-04-2022 was already alive and had itself already formed a fresh `SAR` off `GREEN2` ("SAR has already occurred for GREEN2. New TZ RED cannot co-occur. Basics"). Fixed via an explicit `Lineage.competitor` pairing (set the moment a fresh anchor actually forms in response to a specific dying lineage's reopening) — gates re-arming on THIS lineage's own paired competitor's aliveness, not on any other lineage in the house (an unrelated, long-dormant lineage elsewhere — e.g. one quietly waiting on a `rear_recovery` target it may never reach — must not block a separate lineage's legitimate first race).
  2. **A lineage's gen dying the same day its anchor ALSO deep-SLs recorded a redundant, separate SL for the gen.** The anchor's own deep SL is total, unconditional lineage death (§2); when it fires the same day as the gen's own SL, the gen's SL is a redundant "descendant" event that adds nothing once the whole lineage is gone. Confirmed against 04-05-2022: "since TZ RED SL occurred, why need to record its descendant SAR SL?" Fixed by peeking (side-effect-free) whether the anchor would deep-SL today BEFORE processing the gen at all; if so, the gen isn't processed that day and only the anchor's own SL prints.
  3. **A fresh RED1/GREEN1 attach was blocked on the exact day its front's gen also SLs (shallow or deep), even when that day's raw price action satisfied the attach formula.** The attach formula itself (today's H/L/C vs. yesterday's H/L) has nothing to do with the Struct object's post-SL state; blocking it was inconsistent with how an ALREADY-active pullback is allowed to keep resolving through the very same event (§ item above, 01-03-2022). Confirmed against 22-04-2022/23-04-2022: `RED1` should attach 22-04 (alongside `BAR 2 SL`) and resolve to `RED2` the very next day, 23-04 — both were silently missing. Fixed by gating fresh-attach eligibility on the same PRE-today front-stage2 snapshot already used for fresh-gen-formation (`front_stage2_before_today`), not the live post-SL state.
  4. **An already-active pullback was wrongly cut off by a `continue` when its front's gen underwent a shallow-to-deep escalation or a same-day recovery/reform, not just an ordinary SL.** Only the ANCHOR's own deep SL is total, unconditional lineage death that should skip everything else that day (§2/11-01-2022) — a gen transitioning between recovery states is not. This was invisible until fix 3 above started letting RED1/GREEN1 attach in more of these situations. Fixed by explicitly still processing an already-active pullback at each of the three `continue` sites (bar2_recovery escalating to rear_recovery, bar2_recovery's own gen reforming, rear_recovery's gen reforming/escalating) rather than skipping straight past it.
- **Older-lineage precedence (§14 item above) also applies to a YOUNGER, already-existing lineage's ordinary fresh-gen-formation off the shared `gen_pending` signal, not just to a brand-new fresh ANCHOR forming from scratch.** Confirmed by the user against 02-03-2022 ("REAR BUY + BAR" — only `REAR BUY` should print): when an older lineage's `rear_recovery` resolves the SAME day a younger lineage would independently form its own next gen off `gen_pending`, the two would have "the same future impact" from there (own "2" → SL → RED1/GREEN1 → reform, whatever the name) — the younger lineage's formation is FORFEITED, same as the top-level fresh-anchor case, not merely deferred: it is blocked only for the CURRENT `gen_pending` episode (`Lineage.declined_gen_pending_episode`, compared against a `gen_pending_episode` counter bumped each time a fresh RED2/GREEN2 sets `gen_pending`), so a genuinely NEW `gen_pending` later still gives it a fair shot, but it cannot silently retry off the same stale signal on a later day. This shifted several downstream 2022 dates: 02-03 through 06-03-2022 (`BAR` suppressed alongside `REAR BUY`'s own reform/HH/LL/SL), 29-04/30-04-2022 (`SAR`/`SAR SL` suppressed alongside `REAR SELL`'s own reform/SL), and — as a further ripple from that — 03-05-2022 (the `TZ RED` that previously formed there no longer does).

  These fixes changed a small number of previously-shown dates in the 2021 case-study dataset too (12-04-2021 through 15-04-2021: `RED1`/`RED1 HH`/`RED1 SL` now attach and resolve alongside `BAR 2 SL`/`BAR 2`, the same class of fix as item 3/4 above) — the 2021 dataset has still only been spot-checked, not walked through date-by-date under this latest correction; the note in the anchor-two-tier item above about a full re-verification pass still stands.
- **Two more related fixes, both confirmed by the user against 24-04-2022/25-04-2022:**
  1. **The deep/outer threshold escalation check was silently abandoned the moment `gen_pending` preempted a pending `bar2_recovery` into a full fresh restart.** The old fix for "if RED2 occurs but BAR2 SL is pending, NEW BAR will start" nulled `bar2_recovery` outright the instant `gen_pending` fired — but the deep threshold it tracks (whether BAR's own OUTER reference is ALSO breached) is real, independent price information that must keep being checked regardless of which reform path eventually applies. Confirmed against 24-04-2022: the deep threshold (329) was genuinely breached that day and `BAR SL` was wrongly missing. Fixed by keeping `bar2_recovery` alive (still checking `escalates` every day) alongside `gen_fresh_pending`; only the "recovers" (successful lighter reform) outcome specifically defers to the ordinary fresh-restart trigger instead of reforming at the recovery's own level. (A related fix was needed alongside this: `gen_fresh_pending` must be cleared the moment an escalation DOES fire into `rear_recovery`, or the same lineage's ordinary fresh-gen-formation trigger could independently fire later while its `rear_recovery` is still separately pending — confirmed via a 2021 regression check that briefly resurfaced with this fix and was caught before landing.) This surfaced the identical class of previously-hidden bug in the 2021 dataset too: `SAR HH`/`SAR 2 HH`/`SAR SL` now correctly appear 01-04-2021/03-04-2021, numerically confirmed the same way.
  2. **A fresh RED1/GREEN1 attach must NOT start on the exact day its own front's gen deep-SLs — only on a SHALLOW SL day (or no SL at all).** The earlier fix (item 3 above) treated shallow and deep SLs the same for fresh-attach eligibility; the user drew the line more precisely: a deep SL is total generation failure (opening the REAR-ladder/fresh-anchor dual-track) and a brand-new pullback has no live front worth starting to track against that same day, whereas a shallow SL (the lighter, still-recoverable tier) still has enough continuity for a fresh attach to make sense — confirmed against 25-04-2022 ("SAR SL occurred, then why GREEN1? Deep SL then what is the point of GREEN1?"). This does NOT touch an ALREADY-active pullback continuing/ratcheting through a deep SL, which stays correct as established (01-03-2022, GREEN2 alongside SAR SL) — only a FRESH attach starting that same day is now blocked. Implemented via a per-lineage `gen_deep_sl_today` flag gating only the fresh-attach branch.
- **A fresh RED1/GREEN1 attach was ALSO wrongly gated on `gen_pending` house-wide, blocking an independently-eligible lineage's own front for as long as ANY unrelated lineage's RED2/GREEN2 signal sat unconsumed (which can be weeks).** Confirmed by the user against 01-03-2022 ("TZ GREEN 2 completed, hence RED1 and RED2 is valid"): `TZ GREEN2` (a dual-track lineage completely unrelated to the older `REAR BUY`-ladder lineage the earlier answer focused on) had been independently eligible (`front_ok_for_attach`) since 01-03-2022, but was blocked from ever attaching a fresh RED1 through 07-03-2022 solely because `gen_pending` — set by an unrelated RED2 back on 17-02-2022 and never independently consumed by anything else — was still sitting `True`. `gen_pending` is a per-HOUSE shared flag with no relationship to any one lineage's own eligibility; gating fresh attach on it was simply wrong. Removed the `not gen_pending` condition from the fresh-attach branch entirely (the `gen_pending`-driven fresh-GEN-formation trigger is unaffected, and remains correctly gated). This is a substantial fix, surfacing many previously-missing RED1/RED2/GREEN1/GREEN2 attaches across BOTH case-study datasets (e.g. 2021: 11-01, 24-01–26-01, 10-02–11-02, 19-02–22-02, 03-03, 08-03, 20-03–22-03) that has not yet been walked through date-by-date with the user.
- **Older-lineage precedence was extended to cover two DIFFERENT lineages that would BOTH independently form a plain gen off the SAME `gen_pending` signal on the SAME day (not just a younger lineage colliding with an OLDER lineage's `rear_recovery` resolving, already covered above).** Since a gen_pending-driven reform always resets to the plain name (`recovery_label = None`) regardless of a lineage's prior ladder history, two such lineages forming the same day would always produce the IDENTICAL name and an identical future shape — confirmed by the user against 18-05-2022 ("2 different lineages were at the same occurrence of SAR — mention the earlier one"): only the OLDER lineage's `SAR` now prints.
- **A lineage that loses the "both reach the IDENTICAL plain gen" collision (only that kind — see the correction below) must never independently start a FRESH pullback again either, not merely decline that one gen-formation retry.** The first version of this fix only set `declined_gen_pending_episode` (blocking a re-try off the same stale `gen_pending`), but the losing lineage's own front kept independently attaching fresh RED1/GREEN1 pullbacks afterward, dual-tagged against the winner — confirmed wrong by the user against 21-05-2022/23-05-2022 ("2 different lineages were at the same occurrence of SAR... why have mentioned both?"): once both would reach the literal identical name via the identical mechanism, their futures are indistinguishable from that point on, so a fresh attach must be permanently blocked too (`Lineage.superseded`, gating only the fresh-attach branch — an already-active pullback, and the lineage's own anchor/gen HH/LL/SL tracking, are unrelated and stay unaffected). An earlier, broader attempt at this fix marked the loser fully `dead` instead, which had unintended, wide-reaching side effects through the loser's role in competitor-pairing timing elsewhere and was reverted before landing; `superseded` is deliberately narrower.
- **Correction: `superseded` must NOT also apply to the OTHER, older-lineage-precedence collision type (an older lineage's REAR-ladder reform vs. a younger lineage's would-be plain gen, e.g. 02-03-2022's `REAR BUY` vs. `BAR`) — only a per-day decline (`declined_gen_pending_episode`) belongs there.** That collision is provisional, not a declaration of shared identity: the older lineage (REAR BUY/SELL) hasn't necessarily reached its own "2" yet, so nothing guarantees it will ever independently do what the younger lineage's own front might do later. Confirmed wrong by the user against 04-03-2022 (and the identical principle at 21-01/22-01-2022, 23-03/24-03-2022, 17-04/18-04-2022 — see the next item): `TZ GREEN` (declined on 02-03-2022 in favor of `REAR BUY`) must still independently attach its own `RED1` on 04-03-2022, since `REAR BUY` never reaches `REAR BUY 2` before dying (06-03-2022) and so never actually collides with anything TZ GREEN does — "if REAR BUY 2 would have occurred before 04-03, then excluding TZ GREEN makes sense [...] here the earlier lineage reached only REAR BUY. REAR BUY 2 is still pending." `RED1`/`RED1 LL`/`RED2` now correctly reappear 04-03 through 06-03-2022 alongside `REAR BUY`'s own unrelated events.
- **A front whose own RED2/GREEN2 has ALREADY confirmed once cannot host a second, fresh RED1/GREEN1 attach without first changing identity (a genuinely new Struct forming for that lineage) — this is unrelated to older-lineage precedence entirely; it's a single lineage's own front being asked to do two separate pullback cycles with no new generation in between.** Confirmed by the user against 11-02-2022: `RED2` already confirmed 10-02-2022, "there is no BUY event in between, hence RED1 and RED2 not possible" on 11-02-2022 against the SAME, unchanged `BAR2`. Implemented via `Lineage.pullback_used`, storing the exact front object whose RED2/GREEN2 confirmed; compared by object identity so a later front change (any reform/formation path) naturally reopens eligibility. Tracing this same principle surfaced three more, previously-unflagged instances of the identical bug: 21-01/22-01-2022 (`RED1`/`RED1 SL` incorrectly re-attaching after 20-01-2022's `RED2`), 23-03/24-03-2022 (`RED1`/`RED2` after 20-03-2022's `RED2`), and 30-03-2022 (`GREEN1` after 28-03-2022's `GREEN2`) — all now correctly removed.
- **Once `gen_pending` has already foreclosed `bar2_recovery`'s lighter "NEW BAR2 reforms directly" outcome (`gen_fresh_pending`), its routine ratchet EVENTS (`{base} LL`/`HH`, `INVALID {base} LL`/`HH`, `{base} 2 LL`/`HH`) must stop printing, even though the underlying reference VALUES still update silently (still needed to feed the deep-threshold escalation check).** Printing them implied an active, meaningful recovery target that no longer exists once a full fresh restart is the only remaining lighter-tier outcome. Confirmed by the user against 18-02-2022 ("BAR 2 SL already triggered [17-02-2022], how is BAR 2 LL now possible? This is very basic."): `BAR 2 LL` no longer prints there.
- **A gen's own inner HH/LL ratchet must NOT be suppressed just because a deep SL ALSO fires the same day.** The post-stage2 deep-SL check returned immediately (`return "deep"`) without ever reaching the ordinary HH/LL ratchet check below it, silently dropping a genuinely independent price fact: the gen's inner ref_low/ref_high can move on a DIFFERENT side of the band than whichever side triggered the SL (e.g. the LOW extending the inner ref_low further down on the SAME day the HIGH breaks the deep/outer threshold upward) — these are two separate axes, not a lesser event superseded by the more severe SL the way shallow-vs-deep SL *naming* is superseded (§6). Confirmed by the user against 26-05-2022 ("every SAR 2 LL is very important. Why did you fail to record?") — SAR2's own ref_low had genuinely moved further that day (315.5, below its prior 316) independent of the unrelated upside SL breakout. Fixed by checking/printing the gen's own `{label} 2 HH`/`{label} 2 LL` ratchet immediately before returning "deep", using the exact same ratchet logic as the ordinary (non-SL) path. This also surfaced two previously-missed instances of the identical gap: 01-03-2022 and 25-04-2022, both now also print a `SAR 2 HH` alongside that day's `SAR SL`. (This fix is scoped to the GEN only, `governed=False` — the ANCHOR's own "2"-suffixed HH/LL/SL still never print at all, per the separate, already-established rule.)
- **A prior generation's `bar2_recovery` must be abandoned the moment this SAME lineage forms a brand-new gen via the ordinary `gen_pending` restart trigger, not left ticking in parallel under the new gen's own current label.** That restart trigger is a completely separate mechanism from `bar2_recovery`'s own escalate/recover/ratchet handling and never touched it, so a still-pending recovery from an earlier, now fully superseded generation could keep silently ratcheting (and, worse, could still independently "recover" later and overwrite the brand-new, unrelated gen). Found via 15-06-2022 printing `INVALID SAR LL + SAR LL` under the same lineage that had already formed a brand-new `SAR` on 12-06-2022 — the stale prior-generation reference was still ticking under the new gen's own current label days after the new gen made it moot. Fixed by clearing `bar2_recovery` at the point the fresh restart succeeds. `INVALID SAR LL` no longer prints there; the current gen's own `SAR LL` ratchet is unaffected.
- **The permanent dual-track (§8) was letting a lineage's OWN stale, already-superseded competitor block it from ever reopening a fresh-anchor search again — and, separately, letting an unrelated, long-forgotten lineage from a much earlier race keep resurfacing indefinitely. Three related fixes, all confirmed by the user against 17-06-2022/24-06-2022/26-05-2022/28-05-2022.**
  1. **A fresh TZ GREEN/TZ RED anchor cannot form on a day where ANY (non-dead) lineage is sitting in `gen_fresh_pending`** — that state means RED2/GREEN2 has already fired for that lineage's dead "2", so its own recovery is guaranteed to be a full fresh restart on the SAME lineage (plain BAR/SAR reforming), never a brand-new competing anchor. Confirmed by the user: "if a REAR 2 has occurred, or SAR 2 has occurred and there is an SL of [that] 2nd and there is GREEN 2, then TZ GREEN CANNOT OCCUR." Against 24-06-2022: an unrelated lineage formed all the way back on 08-02-2022 had an independently-pending fresh-anchor search (open since its own REAR SELL died pre-its-own-"2" on 20-06-2022) that coincidentally resolved into a fresh `TZ RED` the SAME day the actually-current lineage's `SAR 2 SL` (23-06-2022, itself already past GREEN 2) forced its own guaranteed full restart into plain `SAR` — the `TZ RED` must not print at all that day. Fixed via `blocked_by_gen_fresh_pending`, gating the top-level fresh-anchor-search block alongside the existing `blocked_by_older_recovery`.
  2. **The moment ANY lineage re-establishes itself past its own "2" (gen or anchor), every OTHER lineage's still-pending recovery search that is NOT its own directly-paired dual-track competitor is now retired outright — it belongs to an older, already-superseded race.** Without this, a lineage formed 08-02-2022 stayed alive with a pending `rear_recovery` for over four months, resurfacing as a `REAR SELL` reform on 17-06-2022 ("Already SAR is active for a current lineage... There is no reason or logic for awakening the REAR SELL on this day") long after the actually-current lineage (formed 13-04-2022) had already re-reached its own SAR 2 multiple times over. Fixed via `retire_other_pending(winner)`, called at every point a lineage's gen or anchor newly reaches its own "2" (the ordinary breakout, and the `bar2_recovery` direct-reform path) — exempting only `winner`'s own paired `Lineage.competitor` (checked both directions), since that specific dyad staying alive at once for as long as it takes is the confirmed, intentional dual-track (e.g. 26-05 through 09-06-2022: the paired fresh `TZ RED` reaching `TZ RED 2` on 29-05-2022 does NOT retire the original lineage's own later `REAR SELL`/`REAR SELL 2` reform on 03-06/04-06-2022 — "REAR SELL AND REAR SELL 2 OCCURRED AND WILL BE APPLIED").
  3. **Two supporting corrections needed for fix 2 above to work without collateral damage, both confirmed against the SAME 2022 window:** (a) when a fresh anchor's search is opened by an anchor's own UNCONDITIONAL total death (not a gen-deep-SL/bar2_recovery-escalation, which always pairs via `pending_competitor_source`) and some OTHER lineage already holds a currently-pending recovery search, the new anchor is now paired with it as `.competitor` too — otherwise that still-relevant search gets wrongly retired the first time the new anchor reaches its own "2" (confirmed against 06-05-2022/07-05-2022: a fresh `TZ RED` spawned from an unrelated anchor's total death reaching `TZ RED 2` the very next day was wrongly erasing the actually-current lineage's own legitimate, still-pending `rear_recovery`, silently deleting the later, correct `REAR SELL` reform). (b) A competitor that has permanently lost an older-lineage-precedence collision (`Lineage.superseded`) is now treated as equivalent to dead for the re-arming gate (`lin.competitor is None or lin.competitor.dead or lin.competitor.superseded`) — nothing ever sets a superseded lineage's own `dead` flag, so its anchor sits "alive" forever and, without this, permanently blocks its paired lineage from ever reopening a fresh-anchor search again. Confirmed against 26-05-2022/28-05-2022: the lineage formed 06-05-2022 was superseded on 18-05-2022 (lost the 18-05-2022 SAR-formation race, per the existing `superseded` rule) but its still-`alive` anchor was wrongly blocking the legitimate fresh `TZ RED` that needed to reopen and form again on 28-05-2022.
  4. **`retire_other_pending`'s exemption of `winner`'s own paired competitor is conditioned on that competitor having ALREADY independently reached its own "2" at some point — not granted unconditionally.** The original version exempted the direct pair unconditionally, regardless of progress; the user clarified the real rule: whichever side of a paired dyad reaches "2" FIRST is permanently safe from then on, no matter which side (older or newer) that turns out to be — but a paired competitor that has NOT YET reached any "2" of its own when the OTHER side gets there first is retired too, exactly like an unrelated, unproven lineage. Confirmed by the user with the SAME 26-05/29-05/04-06-2022 window used to confirm fix 2: the fresh `TZ RED` reaching `TZ RED 2` on 29-05-2022 (already proven at that point) is correctly never retired by the original lineage's own later `REAR SELL 2` on 04-06-2022 ("no need to terminate the new lineage, keep it active") — but a hypothetical fresh competitor that has NOT yet reached any "2" when the older lineage's own reform succeeds first is retired instead ("new lineage will keep recording at the back end"). Implemented via `Lineage.ever_reached_stage2` (set the moment `retire_other_pending` is called for a lineage, i.e. the instant it first reaches its own "2"), and the retirement itself is now unconditional for any other non-exempt lineage — not gated on it currently holding a pending `rear_recovery`/`bar2_recovery` — so a freshly-started, still-unproven competing anchor that hasn't yet reached its own "2" is retired too, not just an already-dormant one. Verified against the full 2022 dataset: identical output to fix 2/3 above, zero additional collateral change.
  5. **A PLAIN gen (first rung, no ladder history — `recovery_label` is `None`) dying BEFORE ever reaching its own "2" now gets a direct reform chance too, correcting a previously-confirmed rule that this was a dead end.** The user's correction: "SAR - SAR SL DOES NOT LEAD TO TZ RED. IMMEDIATELY ANY DAY WHICH IS FULFILLING TZ RED CONDITIONS WILL BE A SAR... SAR - SAR SL - NEW SAR (not necessary wait for SAR ref high)" — a `TZ RED`/`TZ GREEN` anchor formation only ever applies to the house's true FIRST-EVER gen (nothing exists yet to reform from); every later plain-gen pre-"2" death instead reforms directly as the SAME plain gen name (`SAR`/`BAR`), the moment the SAME `formation_break()` breakout that would otherwise form a brand-new anchor fires — not gated on any reference of the dead gen's own at all ("Way one", confirmed explicitly), unlike every other recovery window in this file. This reopens (for plain, non-ladder gens only) what the 15-02-2021 case had established as a dead end for a first-time formation with nothing prior to reform from — that case is unaffected, since the very first gen a house ever forms still has no prior gen to reform against. A genuinely fresh, unrelated `TZ RED`/`TZ GREEN` can still start in parallel exactly as before ("this does not stop new lineage to start... record both events"), racing this direct reform under the same `ever_reached_stage2`-gated termination rule as fix 4 above. Confirmed against 24-06-2022/26-06-2022: a plain `SAR` formed 24-06-2022 died pre-its-own-"2" on 25-06-2022 ("SAR SL"), and the 26-06-2022 breakout — which the code previously formed as a fresh `TZ RED` — now correctly reforms directly as plain `SAR` on the SAME lineage instead. Implemented by giving the pre-"2" death branch a `rear_recovery` entry tagged `direct_reform: True`; `recovery_would_resolve()` dispatches such an entry to `formation_break()` instead of the ordinary single-threshold breach, its ratchet-while-awaiting is skipped entirely (no reference of its own to track), and on success `recovery_label` is reset to `None` (not the literal gen name) so a LATER escalation still correctly treats this as the first rung, not ladder history. Verified against the full 2022 dataset: the only change anywhere is 26-06-2022 (`TZ RED`→`SAR`) and its outer-ratchet relabeling on 29-06-2022 (`TZ RED LL`→`SAR LL`) — nothing else shifts.
  6. **All threshold arithmetic in this file now uses exact `Decimal` values instead of raw binary floating point, per the user's explicit correction.** `THRESH`/`ANY` and every OHLC value are converted to `Decimal` (via `Decimal(str(x))`, the safe pattern for a decimal-literal source) at both the module level and defensively again inside `run_house` itself; every `-`/`+`/comparison downstream is then exact for these 2-3-decimal-place prices, with zero changes needed to the comparison logic itself. This was NOT a cosmetic concern: against a later data correction (27-06-2022 High revised to 311.7, against a SAR/BAR gen whose `ref_high` was exactly 311.5), the difference is exactly `0.20` in decimal — a tie against `THRESH`, which should read as an SL — but raw Python floats compute `311.7 - 311.5` as `0.19999999999998863`, a hair under `0.20`, silently flipping the result from `SAR SL`/`BAR SL` to `SAR HH`/`BAR HH` and changing the entire trajectory from that date forward. Confirmed by the user: "WRONG. NO FLOATING ARITHMETIC... 27/06 diff is .20. Hence no HH. It changes everything." One supporting fix was needed alongside this: `process_pullback`'s RED2/GREEN2 equality-tolerance literal (`+ 0.001` / `- 0.001`) also had to become `Decimal("0.001")`, since a raw float there would fail to add/subtract against Decimal operands at all (`TypeError`), not just risk imprecision. Verified against the full 2022 dataset: no other date changes from this alone — the only difference from decimal arithmetic shows up at the exact 27-06-2022 boundary the user flagged, with the naturally cascading trajectory change that follows from it.
- **Five related fixes, all confirmed by the user against the full 2021 case-study dataset (01-01-2021–24-04-2021), shared and previously untested by the 2022 dataset:**
  1. **A gen's deep-SL "also breaches the OUTER threshold" ratchet (§6) only ever independently proves the side OPPOSITE whichever side triggered the SL — the SAME side is automatically implied (breaching the more extreme outer threshold necessarily also breaches the closer inner one on that same side) and printing it restates the SL that already fired.** This corrects the 26-05-2022 fix (§14 above), which checked both sides unconditionally — what actually made 26-05-2022 correct was that `SAR 2 LL` there was the OPPOSITE side of the High that triggered the SL, never the same side. Confirmed by the user against 24-02-2021 ("BAR SL TRIGGERS THAN WHY ITS DECENDENTS LL is getting recorded? BAR 2 LL will have no impact in such Instance. Deep SL has triggered"): `BAR SL + BAR 2 LL + RED2` → `BAR SL + RED2`. Re-verified against the full 2022 dataset: `SAR SL + SAR 2 HH + SAR 2 LL` (26-05-2022) correctly becomes `SAR SL + SAR 2 LL` (High triggered the SL there, so `SAR 2 HH` is now the suppressed same side); `SAR SL + SAR 2 HH + GREEN2` (01-03-2022) and `SAR SL + SAR 2 HH` (25-04-2022) lose their same-side `SAR 2 HH` the same way.
  2. **The gen's own shallow-SL branch (§7 setup) checks and prints BOTH sides unconditionally, exactly like the ordinary (non-SL) ratchet check — unlike deep SL (fix 1 above), the SAME side that triggered a shallow SL is NOT redundant and must still print.** An intermediate version of this fix wrongly extended fix 1's same-side suppression to the shallow tier too (reasoning the same-side value was "just feeding `bar2_recovery`'s `inner_adverse` silently"), but the user reasserted the correction even after seeing `SAR 2 SL` print alone on 03-03-2021: "BEAR: SAR HH not recorded? Such a basic. WHY THIS ERROR?" The two tiers are NOT analogous — a deep SL's same-side reference truly becomes moot (the lineage dies into a completely different tracking mechanism keyed off the FAVORABLE side only), whereas a shallow SL's same-side reference is exactly what `bar2_recovery`'s own `inner_adverse` keeps reading on EVERY subsequent day for its escalation check — a live, ongoing fact worth reporting each time it moves, not a one-off capture into a dead structure. `SAR 2 SL` → `SAR 2 SL + SAR 2 HH + SAR 2 LL` (15-01-2021) and `SAR 2 SL` → `SAR 2 SL + SAR 2 HH` (03-03-2021, the actual date confirmed); the value itself was already being updated correctly either way, only the print was ever at issue.
  3. **The gen_pending-driven fresh-PLAIN-GEN-formation trigger (§14 item 5's sibling mechanism, formation_break-gated) must also be blocked by an OLDER lineage's own `rear_recovery` that is merely still PENDING — not only one actually resolving on the exact same day.** Confirmed by the user against 03-03-2021: TZ GREEN (a younger lineage, formed 01-03-2021, reaching its own TZ GREEN 2 on 02-03-2021) must not consume the shared `gen_pending` signal to form a brand-new plain `BAR` on 03-03-2021 while an OLDER lineage's own `BAR SL` (24-02-2021, itself a post-BAR-2 death) still has its resulting `REAR BUY`-targeting `rear_recovery` outstanding, even though that older recovery is not itself resolving that exact day — "you considered RED 2. What about the BAR SL? [...] hence another branch of BAR is not possible even though RED 2 is there. REAR can occur above the earlier BAR 2 HH [...] it is very basic that BAR SL can trigger even after BAR 2." Implemented by broadening `pending_rear_recovery_created_days` (renamed from `resolving_rear_recovery_created_days`) from "resolving today" to "pending at all," scoped ONLY to this one trigger's own older-lineage-precedence gate — explicitly NOT the top-level fresh-ANCHOR search (`blocked_by_older_recovery`, which stays resolving-today-only per the confirmed, permanent dual-track) and NOT a lineage's own fresh RED1/GREEN1 pullback-attach eligibility (`front_ok_for_attach`, a separate mechanism, unaffected per the 04-03-2022 precedent). This suppresses the entire short, wrongly-formed `BAR`→`BAR 2`→`RED1`→`BAR SL + BAR 2 LL + RED2` arc that previously ran 03-03 through 07-03-2021 under this younger lineage; re-verified against the full 2022 dataset: the ONLY 2022 date affected by this specific change in isolation is a similarly-shaped, previously-unflagged case around 08-03-2022 (see fix 5 below, which is what actually surfaced it) — every other previously-confirmed 2022 date this trigger touches (02-03–06-03, 29-04/30-04, 18-05/21-05-2022 etc.) is unchanged.
  4. **The anchor's own deep-SL condition (§6, the same two-tier check as the gen) must keep being checked EVERY DAY for as long as the anchor itself is alive — including AFTER `anchor_retired` (once the gen has reached its own "2"), not just up to that point.** If breached, it is TOTAL, unconditional lineage death (no REAR possible — only a wholly fresh new lineage can start), taking precedence over whatever the gen's own SL/escalation/recovery would otherwise produce that same day; `anchor_retired` only suppresses the anchor's own ROUTINE outer HH/LL *display* (`mute_ratchet`, new parameter on `process_gen`) — never this deep-SL check, and never the outer ratchet's underlying VALUE, which must keep updating silently so the threshold stays accurate. Confirmed by the user against 16-01-2021: with TZ RED's own last-ratcheted outer threshold at 609.1 (11-01-2021) and that day's High 610 / Close 609.5 both past it, "if TZ RED SL triggers along with SAR SL or after SAR SL, it completely deletes the lineage. Only a new lineage can start. Even if there is a high and close above the SAR 2 ref high, the earlier lineage cannot be active with REAR SELL" — `SAR SL` → `TZ RED SL`. This precedence check had to be moved to the very TOP of each lineage's per-day processing (ahead of the `bar2_recovery` escalation check and the `rear_recovery` resolution check, not merely ahead of the ordinary gen block as an intermediate version of this fix had it) — otherwise, on a day the gen had already died into one of those recovery windows on an earlier day (as had happened here, 15-01-2021), that recovery block's own escalation/resolution claimed the day and `continue`d before the anchor-processing block (further down) ever got a chance to run, producing the wrong event one day late. A second, independent bug surfaced once the anchor could newly reach this total-death path from an `anchor_retired` state: its own `rear_recovery`/`bar2_recovery` (if it had one pending) must be explicitly cleared at the moment of this death, not left stale — two house-wide scans elsewhere (`blocked_by_older_recovery`, and fix 3's `pending_rear_recovery_created_days`) read every lineage's `rear_recovery` regardless of `dead`, so a stale non-None value on a permanently-dead lineage would otherwise block every OTHER lineage in the house from ever forming a fresh gen again, forever, over a recovery that can now never resolve — caught via the full 2022 regression (an 08-02-2022 lineage's long-pending `REAR SELL`-targeting recovery, still alive when its own retired anchor finally deep-SL'd on 09-03-2022, was wrongly left blocking a completely unrelated lineage's fresh `SAR` for the rest of the dataset until this clearing was added). Re-verified against the full 2022 dataset with both fixes together: this rule now also fires two previously-missing total deaths at 26-04-2022 (`TZ RED SL`, ending a rear_recovery that had been quietly pending since 25-02-2022 and voiding its would-be 29-04-2022 `REAR SELL` reform) and 09-03-2022 (a SECOND, independent lineage's `TZ RED SL` the same day as an already-known one — both anchors happen to share the generic house label `TZ RED`, so the output shows the identical text twice, `TZ RED SL + TZ RED SL`, for two distinct lineages; this is a pre-existing display limitation, not a new bug, and is unrelated to the pullback mechanism's own same-day dual-tagging convention).
  5. **A gen's shallow SL escalating into a heavier ladder recovery (`bar2_recovery`'s own escalation check, §7/§8) must still open that recovery when `gen_pending` ALSO becomes true the SAME day the shallow SL fires — not only when `gen_pending` arrives on a LATER day (already fixed, 24-04-2022).** The same-day case had a separate code path that discarded the `bar2_recovery` structure entirely (only setting `gen_fresh_pending`), silently dropping the deep/outer escalation check along with it — exactly the class of bug already fixed for the later-day case, just in an until-now-uncovered sub-case. Confirmed by the user against 04-03-2021: this SAR2's own deep/outer threshold was breached the very next day and the correct `SAR SL` (escalating into `REAR SELL`) was silently missing. Fixed by always creating `bar2_recovery` on a shallow SL regardless of `gen_pending`'s same-day state, with only the transition's own same-side ratchet print (`{label} LL`/`{label} HH`) silenced when `gen_pending` already forecloses the lighter reform outcome (matching the established silencing convention used elsewhere in `bar2_recovery`). Re-verified against the full 2022 dataset: no date is newly affected by this specific sub-case in isolation (2022's own instances of this same-day collision, e.g. 12-06-2022, were already exercised by other paths); the visible 2021 effect is `SAR SL` now correctly appearing 04-03-2021, escalating into the `REAR SELL` reform that resolves 10-03-2021 and reaches `REAR SELL 2` 13-03-2021.
  - Together these five fixes reshape a long stretch of the 2021 output from 16-01-2021 through late April 2021 (the anchor-total-death and pending-recovery-blocking fixes both open and close entire lineage lifespans, which naturally cascades everything downstream of each such lineage) — walked through with the user afresh rather than assumed correct by construction. The isolated, single-date checks above are the parts independently re-verified against exact numbers; the full cascaded table was regenerated and shared with the user for date-by-date review, per this project's standing practice of never asserting a fix is right merely because it changed the output in the expected direction.
  - One 2021 date checked and confirmed NOT a bug during this same round: 03-04-2021's `SAR SL` was flagged by the user as a possible instance of fix 5's "§14 item 5" direct-reform rule (SAR SL → new SAR, confirmed in 2022), but this particular SAR (originally formed 08-02-2021, reformed as SAR 23-02-2021, reaching SAR 2 24-02-2021) had already reached its own "2" well before its 03-04-2021 death — direct reform (§14 item 5) only applies to a plain gen that NEVER reaches its own "2"; a post-"2" death is a legitimate escalation into the REAR-ladder/fresh-anchor dual-track (§8) instead, which is what the code already produced.
- **OPEN, UNRESOLVED CONFLICT — fix 3 above (older-lineage-precedence blocking a fresh-plain-gen-formation) appears to contradict itself once applied to a lineage's own PAIRED dual-track competitor, and the two known cases point opposite ways.** Case A (fix 3's own confirmed basis, 03-03-2021): `TZ GREEN` (formed 01-03-2021, `.competitor`-paired with the original `BAR` lineage via that lineage's own 24-02-2021 post-`BAR 2` death) is correctly blocked from forming its own first `BAR` while the older lineage's `REAR BUY`-targeting `rear_recovery` sits pending — and stays blocked for the rest of the visible dataset, since `REAR BUY` never once resolves. Case B (flagged by the user this round, 22-03-2021 onward): a fresh `TZ RED` (formed 06-03-2021, `.competitor`-paired with the original `SAR` lineage via that lineage's own 04-03-2021 post-`SAR 2` death) is, by the SAME mechanism, blocked from ever forming its own first `SAR` for the entire 08-03–28-03-2021 stretch while the older lineage's `REAR SELL`/`REAR SELL RE ENTER` recovery keeps being pending — but the user says this is wrong ("BEAR: SAR?? BEAR DOES NOT HAVE ANYTHING. WHY. THERE IS AN ERROR WITH BAR AND SAR FROM THIS DATE"). Both pairings were formed identically (a gen's post-stage2 death opening a `rear_recovery` plus a fresh-anchor search, the new anchor paired via the standard `pending_competitor_source` mechanism) and both younger lineages had already reached their own anchor's "2" before the block took effect — no mechanical difference has been found between the two cases. The one substantive difference identified so far: in Case B, the older lineage's recovery had ALREADY successfully resolved once before (SAR SL → REAR SELL formed 10-03-2021 → reached REAR SELL 2 13-03-2021) before re-opening into its second, currently-pending escalation (REAR SELL RE ENTER); in Case A, the older lineage's recovery has NEVER once resolved. Whether "has the older lineage's recovery ever previously succeeded" is the actual distinguishing rule (exempting a paired competitor once the older side proves the recovery isn't a permanent dead end) is unconfirmed — this needs the user's clarification before either the 03-03-2021 block or the March-2021 SAR block can be touched again. An exemption keyed on `Lineage.competitor` alone was tried and reverted: it fixes Case B but reopens Case A (`BAR` wrongly reforms 03-03-2021 again).
- Whether the $0.20 / $0.01 thresholds need to scale for weekly/monthly/yearly candles remains an open question inherited unchanged from the original rule book (§13 there).
- Reference implementation: `bar_rule_simulator.py` (this repo), run against both case-study datasets (01-01-2021–24-04-2021, 01-01-2022–10-03-2022). Not yet merged into `tz_engine_v9.py` as a production rule variant.

## 15. Support/resistance highlighting

A separate reporting feature, layered on top of the events above without changing any mechanic: highlight the currently-relevant support (Bull) or resistance (Bear) price level alongside the OHLC table.

- **Bull support (green) = the Low of whichever formation event (`TZ GREEN`, `BAR`, `REAR BUY`, `REAR BUY RE ENTER`) or base-level `LL` suffix on one of those names (`TZ GREEN LL`, `BAR LL`, `REAR BUY LL`, `REAR BUY RE ENTER LL`) is most recently active — NEVER shifted by a "…2" event.** `BAR LL`/`REAR BUY LL` are exactly the gen's own outer-reference ratchet, which only ever fires within a `bar2_recovery`/`rear_recovery` awaiting window (§7/§8) since the gen's outer otherwise stays frozen (§5/§6); `TZ GREEN LL` is the anchor's own outer ratchet, which now fires continuously post-`TZ GREEN 2` (§2).
- **Bear resistance (red) mirrors this on the High side**: `TZ RED`/`SAR`/`REAR SELL`/`REAR SELL RE ENTER` formation events and their base-level `HH` suffixes.
- Only the High or Low cell itself is highlighted for a given date — not the whole row/column — and the highlighted value updates every time a new qualifying event fires, revising forward from that date.
- Presentation in a plain-text chat reply uses 🟢/🔴 emoji markers next to the highlighted value (color spans/HTML do not render in this chat surface); an HTML artifact or image can use literal color (light green/light red) instead, per the user's preference for a given reply.
